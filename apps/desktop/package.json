{
  "name": "deskmakeover-desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "tauri:build": "tauri build",
    "gen:bindings": "cargo run -p deskmakeover-desktop --bin gen-bindings",
    "check:bindings": "cargo test -p deskmakeover-desktop --test bindings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
